#!/bin/sh

perms=$*
if test -z "$perm"; then
	perms="600 666"
fi
for perm in $perms; do
	ipcs -m | grep "^0x.* $USER $perm " |
	awk -F' ' '{print $2}' |
	while read line; do
		echo "Removing $line"
		ipcrm -m $line;
	done
done
