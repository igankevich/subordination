subordination_core_src += files([
    'error.cc',
    'error_handler.cc',
])

install_headers(
    'error_handler.hh',
    'error.hh',
    'static_lock.hh',
    subdir: join_paths(meson.project_name(), 'base')
)
